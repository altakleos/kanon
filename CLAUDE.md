See @AGENTS.md

**Do not use Claude's built-in project memory.** Do not write to or rely on `~/.claude/projects/<slug>/memory/` (or any analogous auto-memory location). Persistent guidance for this repo lives in `AGENTS.md` and the `.kanon/protocols/` tree, both checked into git and reviewable. The auto-memory system is opaque to other contributors and other agents and is therefore disallowed here.
