The `kanon-fidelity` aspect is enabled at depth 0 (opt-out). No behavioural-conformance verification is active. To enable lexical-replay fidelity checks, raise the depth:

```bash
kanon aspect set-depth . fidelity 1
```
