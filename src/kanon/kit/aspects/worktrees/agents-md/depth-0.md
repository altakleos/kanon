The `worktrees` aspect is enabled at depth 0 (opt-out). No worktree guidance or scripts are active. To enable worktree isolation for parallel work, raise the depth:

```bash
kanon aspect set worktrees --depth 1
```
