---
feature: v0.2-aspect-model
serves: docs/specs/aspects.md
design: docs/design/aspect-model.md
status: planned
date: 2026-04-23
---
# Plan: v0.2 — Aspect model refactor (SDD becomes the first aspect)

## Context

Promote aspects to first-class citizens in the kit per ADR-0012 + ADR-0013, with `sdd` as the single shipped aspect at this milestone. Zero user-visible behaviour change for existing v0.1 consumers: `kanon init foo --tier 2` continues to produce an identical tier-2 project; `kanon tier set` continues to work as sugar; legacy `.kanon/config.yaml` auto-migrates to schema v2 on first `upgrade`.

A follow-up plan introduces the first non-SDD aspect (`worktrees`) once this refactor lands — per the `solo-with-agents` persona, worktrees is the first aspect kanon's agent-first user pulls in.

See `docs/design/aspect-model.md` for the architecture this plan executes against.

## Phases

### Phase α — Foundational artifacts (drafts on disk, pending commit)

- [ ] T1: `docs/specs/aspects.md` (draft status) — on disk, needs commit
- [ ] T2: `docs/decisions/0012-aspect-model.md` (provisional) — on disk, needs commit
- [ ] T3: `docs/decisions/0013-vision-amendment-reference-automation.md` (provisional) — on disk, needs commit
- [ ] T4: `docs/foundations/personas/solo-with-agents.md` — on disk, needs commit
- [ ] T5: `docs/design/aspect-model.md` (this design) — on disk, needs commit
- [ ] T6: Index updates — `docs/decisions/README.md`, `docs/foundations/personas/README.md` — on disk, needs commit
- [ ] T7: Edit `docs/foundations/vision.md` in place per ADR-0013 after-state; bump `date:` frontmatter
- [ ] T8: Update `docs/specs/README.md` — add `aspects.md` row (either "In-progress specs" section or similar)
- [ ] T9: Promote spec and both ADRs `draft|provisional` → `accepted` after Phase α commit

### Phase β — Producer-side layout refactor (one atomic commit)

- [ ] T10: `mkdir -p src/kanon/kit/aspects/sdd/{agents-md,sections,protocols,files}`
- [ ] T11: `git mv src/kanon/kit/agents-md/* src/kanon/kit/aspects/sdd/agents-md/`; rename each `tier-N.md` → `depth-N.md`
- [ ] T12: `git mv src/kanon/kit/sections/* src/kanon/kit/aspects/sdd/sections/`
- [ ] T13: `git mv src/kanon/kit/protocols/* src/kanon/kit/aspects/sdd/protocols/`
- [ ] T14: `git mv src/kanon/kit/files/* src/kanon/kit/aspects/sdd/files/`
- [ ] T15: Edit each `aspects/sdd/agents-md/depth-N.md` to use namespaced marker prefixes (`sdd/plan-before-build`, `sdd/spec-before-design`); keep `protocols-index` unprefixed
- [ ] T16: Write `src/kanon/kit/aspects/sdd/manifest.yaml` (per-depth files/protocols/sections)
- [ ] T17: Rewrite `src/kanon/kit/manifest.yaml` to aspect-registry shape (top-level `aspects: {sdd: {...}}`)
- [ ] T18: Update protocol frontmatter — `tier-min: N` → `depth-min: N` across `tier-up-advisor.md`, `verify-triage.md`, `spec-review.md`

### Phase γ — CLI refactor (atomic with Phase β or immediately after)

- [ ] T19: Replace `_load_manifest()` with `_load_top_manifest()` + `_load_aspect_manifest(aspect)` (both cached via `lru_cache`)
- [ ] T20: Rewrite `_expected_files`, `_build_bundle`, `_assemble_agents_md`, `_render_protocols_index` to take `aspects: dict[str, int]` instead of `tier: int`
- [ ] T21: Add `_migrate_legacy_config()` transformer; call from `upgrade` and `_read_config` when `aspects:` key absent
- [ ] T22: Add `kanon aspect` Click subgroup with `list`, `info`, `add`, `remove`, `set-config`, `set-depth`
- [ ] T23: Preserve `kanon tier set <target> <N>` as backwards-compat sugar for `kanon aspect set-depth <target> sdd <N>`
- [ ] T24: Rewrite `_write_config` to emit schema v2 `aspects: {…}`
- [ ] T25: `kanon init --tier N` routes internally through `--aspect sdd --depth N`

### Phase δ — Consumer-side migration inside `upgrade`

- [ ] T26: `upgrade` detects flat `.kanon/protocols/*.md` layout and moves files under `.kanon/protocols/sdd/` atomically (mirrors `_atomic_replace_dir`)
- [ ] T27: `upgrade` rewrites AGENTS.md marker sections — `plan-before-build` → `sdd/plan-before-build`, `spec-before-design` → `sdd/spec-before-design`; `protocols-index` unchanged
- [ ] T28: `upgrade` emits an info-level notice summarizing the migration (`Migrated legacy tier config to aspect model.` + `Namespaced 3 protocols under sdd/.`)

### Phase ε — CI validator updates

- [ ] T29: `ci/check_kit_consistency.py` learns aspect-registry shape; walks `aspects.<name>.path` and loads each sub-manifest
- [ ] T30: Add per-aspect path-resolution check (every sub-manifest path resolves)
- [ ] T31: Add cross-aspect file-ownership exclusivity check
- [ ] T32: Byte-equality whitelist rewritten for new paths (under `aspects/sdd/`)
- [ ] T33: Add stability-label validator (`stability ∈ {experimental, stable, deprecated}`)
- [ ] T34: Add section-namespace discipline check (every marker pair uses `<aspect>/<section>` prefix, except `protocols-index`)

### Phase ζ — Test updates

- [ ] T35: `tests/test_cli.py` — parametrise existing tier tests on `(aspect, depth)` pairs; add `test_init_aspect` happy-path tests
- [ ] T36: `tests/test_kit_integrity.py` — aspect-registry shape, per-aspect sub-manifest resolution, cross-aspect ownership
- [ ] T37: `tests/test_protocols.py` — update paths for `.kanon/protocols/sdd/*.md` layout; verify `depth-min` frontmatter
- [ ] T38: New `tests/test_aspect_migration.py` — legacy v1 → v2 config round-trip; flat-protocols → namespaced round-trip; AGENTS.md marker-rename idempotence
- [ ] T39: New `tests/test_aspect_lifecycle.py` — `aspect add/remove` non-destructive round-trip, `aspect info` output shape, `aspect list` catalog shape
- [ ] T40: Verify coverage ≥ 70% post-refactor (may need to raise floor once stable)

### Phase η — Self-hosting (this repo as tier-3 kanon consumer)

- [ ] T41: Run `python -m kanon upgrade .` against this repo; legacy `tier: 3` config auto-migrates to `aspects: {sdd: {depth: 3, …}}`
- [ ] T42: Verify this repo's `AGENTS.md` renders with namespaced markers (`sdd/plan-before-build` etc.)
- [ ] T43: `kanon verify .` returns `status: ok` at the new schema
- [ ] T44: This repo's `.kanon/protocols/*.md` move under `.kanon/protocols/sdd/` — verify intra-repo references still resolve

### Phase θ — CHANGELOG + release prep

- [ ] T45: Append `[Unreleased]` entry to `CHANGELOG.md` — Added (aspect model, `kanon aspect` subgroup), Changed (kit layout, protocol namespacing), Migration (auto v1→v2 config)
- [ ] T46: Run `ruff check`, `mypy --strict`, all four CI validators, full pytest suite
- [ ] T47: Version bump `src/kanon/__init__.py` → `0.2.0a1` (first v0.2 alpha)
- [ ] T48: Dead-ref grep — `rg 'kit/agents-md|kit/sections|kit/protocols|kit/files'` in repo returns only intentional references
- [ ] T49: Dry-run pip wheel build + `ci/check_package_contents.py`

## Acceptance Criteria

- [ ] AC1: All tests pass; coverage ≥ 70%.
- [ ] AC2: `src/kanon/kit/aspects/sdd/` contains all former tier content; v0.1 top-level `kit/agents-md/`, `kit/sections/`, `kit/protocols/`, `kit/files/` directories no longer exist.
- [ ] AC3: Self-verify at depth-3 passes via `kanon verify .` against this repo.
- [ ] AC4: `kanon init --tier N`, `kanon tier set`, `kanon upgrade`, `kanon verify` remain backwards-compatible for v0.1-shaped consumer invocations.
- [ ] AC5: Legacy tier config auto-migrates on first `upgrade` with zero user intervention; migration notice visible in output.
- [ ] AC6: `kanon aspect list` returns only `sdd` at this milestone (only stable aspect shipping).
- [ ] AC7: All four CI validators green against the refactored tree.
- [ ] AC8: CHANGELOG `[Unreleased]` entry describes the refactor and auto-migration.
- [ ] AC9: `__version__` bumped to `0.2.0a1` and `pyproject.toml` metadata still builds a valid wheel.

## Out of Scope (deferred to follow-up plans)

- **First non-SDD aspect** (`worktrees`) — proves the model against a real new discipline with agent-first triggers. Separate plan.
- **`versioning` aspect** — second non-SDD aspect. Separate plan.
- **`release` aspect** — first aspect using ADR-0013's reference automation snippets (GitHub Actions templates). Separate plan.
- **Automated fixture replay** (ADR-0005 follow-on) — precondition per ADR-0012 maintenance red lines before aspect count > ~5.
- **Third-party aspect registry** — remains explicitly out-of-scope per aspects spec; revisit post-v1.0.
