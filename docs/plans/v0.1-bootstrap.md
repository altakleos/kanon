---
feature: v0.1-bootstrap
serves: docs/foundations/vision.md
design: TBD
status: done
date: 2026-04-22
---
# Plan: Bootstrap `kanon` v0.1

## Context

This plan records the execution of the v0.1 bootstrap — the first release of the portable, self-hosting SDD kit. The full design memo (including agent debate, counter-arguments, and resolved scope) lives in the session plan at `~/.claude/plans/implement-1-thru-7-whimsical-backus.md`; this file is the in-repo execution record.

The bootstrap resolves a chicken-and-egg: the kit requires itself to develop itself. The resolution is a named pre-SDD bootstrap phase (commits 1–3), documented in ADR-0002, after which SDD engages for all subsequent work.

## Execution phases

### Phase A — Bootstrap (commits 1–3, pre-SDD)

- [x] T1: Repo skeleton — `LICENSE`, `pyproject.toml`, `README.md`, `CHANGELOG.md`, `AGENTS.md`, `CLAUDE.md`, `.gitignore`, `src/kanon/__init__.py` → repo root
- [x] T2: Port `docs/development-process.md` from Sensei (de-Sensei-fied — rename `sensei-implementation.md` → `kanon-implementation.md`) → `docs/development-process.md`
- [x] T3: Index stubs — `docs/foundations/README.md`, `docs/decisions/README.md`, `docs/plans/README.md`, `docs/plans/roadmap.md`, `docs/foundations/vision.md` (bootstrap stub), `docs/kanon-implementation.md` (bootstrap stub) → linked from AGENTS.md so no broken links at commit-1 CI time
- [x] T4: Flesh out `docs/foundations/vision.md` — Current Promises, Non-Goals, what the kit is and isn't → commit 2
- [x] T5: Author eight critical ADRs (0001–0008) per the session plan → commit 3
- [x] T6: Populate `docs/decisions/README.md` index with all eight ADRs → commit 3

### Phase B — Foundations & specs (commits 4–10, SDD engaged)

- [x] T7: Six principles under `docs/foundations/principles/` (`P-prose-is-code`, `P-specs-are-source`, `P-tiers-insulate`, `P-self-hosted-bootstrap`, `P-cross-link-dont-duplicate`, `P-verification-co-authored`)
- [x] T8: Three personas under `docs/foundations/personas/` (`solo-engineer`, `platform-team`, `onboarding-agent`)
- [x] T9: Five core specs — `cli.md`, `template-bundle.md`, `cross-harness-shims.md`, `tiers.md`, `verification-contract.md`, `tier-migration.md` (note: 6 specs total, not 5 — spec list updated to include `tier-migration.md` per ADR-0008)
- [x] T10: Six `status: deferred` specs — `fidelity-lock.md`, `spec-graph-tooling.md`, `ambiguity-budget.md`, `multi-agent-coordination.md`, `expand-and-contract-lifecycle.md`, `invariant-ids.md`
- [x] T11: Update `docs/plans/roadmap.md` to link every deferred spec (placeholder table populated in commit 1)
- [x] T12: `docs/design/template-bundle.md` — file-level design for all four tier bundles

### Phase C — Implementation (commits 11–18)

- [x] T13: Port `_atomic.py` from Sensei → `src/kanon/_atomic.py`
- [x] T14: Write `src/kanon/cli.py` — `init`, `upgrade`, `verify`, `tier set`, `--version`
- [x] T15: Build four tier templates at `src/kanon/templates/tier-{0,1,2,3}/` — tier-3 canonical, tier-{0,1,2} strict subsets
- [x] T16: Write per-tier AGENTS.md section fragments at `src/kanon/templates/agents-md-sections/tier-{0,1,2,3}.md`
- [x] T17: Write the section-marker rewriter (idempotent, non-destructive) used by `init` and `tier set`
- [x] T18: Write `src/kanon/templates/harnesses.yaml` — shim registry
- [x] T19: Write `src/kanon/templates/tier-0/.kanon/config.yaml` seed (and equivalents for other tiers)

### Phase D — Verification (commits 19–25)

- [x] T20: Port `ci/check_foundations.py` (adapted to accept `status: deferred`)
- [x] T21: Port `ci/check_links.py`
- [x] T22: Port `ci/check_package_contents.py` (adapt `REQUIRED_FILES`/`FORBIDDEN_PREFIXES`)
- [x] T23: Write `ci/check_template_consistency.py` — tier-3 ↔ repo byte-equality + tier-subset invariant + AGENTS.md marker balance
- [x] T24: Write `tests/test_cli.py` — init/upgrade/verify/tier atomicity + tier-migration round-trip
- [x] T25: Write `tests/test_template_integrity.py` (parametrized over tier 0-3)
- [x] T26: Write `tests/test_shims.py`
- [x] T27: Write `tests/ci/test_check_*.py` for each validator

### Phase E — CI + release (commits 26–28)

- [x] T28: `.github/workflows/verify.yml`
- [x] T29: `.github/workflows/release.yml` with OIDC trusted publishing
- [x] T30: Tag `v0.1.0a1` and cut the first release

### Phase F — Dogfooding checkpoint (commit 29+)

- [x] T31: `kanon verify .` against this repo returns OK (tier-3)
- [x] T32: `kanon init /tmp/test-tier{0,1,2,3}` produces valid scaffolded projects; each passes `kanon verify`
- [x] T33: Tier-migration chain smoke — `init --tier 0`, then `tier set` through 1→2→3, then back. User-added files survive.

## Acceptance Criteria

- [x] AC1: All self-hosting assertions from the session plan hold (see `~/.claude/plans/...`):
  1. `kanon verify .` OK on this repo (tier-3)
  2. `kanon init <target> --tier <N>` + `verify` OK for all four tiers
  3. Tier-3 template parity (byte-equality with repo canonical sources)
  4. Tier subset invariant (tier-N ⊆ tier-(N+1))
  5. Discoverability — fresh session can name all 6 deferred capabilities via boot chain
  6. Tier migration round-trip preserves user content
- [x] AC2: CI green — ruff, mypy --strict, pytest (coverage ≥80%), check_foundations, check_links, check_package_contents, check_template_consistency
- [x] AC3: `v0.1.0a1` tag pushed to origin
- [x] AC4: PyPI release published (when PyPI account ready)

## Status notes

Commit 1 (the repo skeleton + dev-process.md + index stubs) has landed. Commits 2 and 3 are in flight.

If the bootstrap is interrupted between commits, the resumption cue is: check `CHANGELOG.md` `[Unreleased]` section and `docs/plans/v0.1-bootstrap.md` checkboxes above.
