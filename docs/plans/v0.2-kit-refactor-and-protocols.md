---
feature: kit-refactor-and-protocols
serves: docs/specs/template-bundle.md, docs/specs/protocols.md
design: docs/design/kit-bundle.md
status: in-progress
date: 2026-04-22
---
# Plan: Kit bundle refactor + three protocols

## Context

Two changes land together (under `[Unreleased]`; no version bump):

1. **`src/kanon/templates/` → `src/kanon/kit/`** — restructure to mimic Sensei's `src/sensei/engine/` shape: one unified bundle with a kernel doc (`kit.md`), tunables YAML (`manifest.yaml`), and sub-buckets (`protocols/`, `files/`, `sections/`, `agents-md/`). Eliminates the ~4× duplication imposed by per-tier directories. See ADR-0011.
2. **Ship three prose-as-code protocols** scaffolded into consumer repos at `.kanon/protocols/`:
   - `tier-up-advisor.md` (tier-1+)
   - `verify-triage.md` (tier-1+)
   - `spec-review.md` (tier-2+)

   See ADR-0010 and `docs/specs/protocols.md`.

The two changes bundle because adding protocols to the old per-tier layout would grow the duplication problem, not solve it. The full design memo (context, alternatives, agent debate, resolved scope) lives in the session plan at `~/.claude/plans/implement-1-thru-7-whimsical-backus.md`; this file is the in-repo execution record.

## Execution phases

### Phase α — Spec + ADRs + plan file (commit 1)

- [x] T1: `docs/plans/v0.2-kit-refactor-and-protocols.md` (this file)
- [x] T2: `docs/specs/protocols.md` — invariants for the new protocol layer
- [x] T3: `docs/decisions/0010-protocol-layer.md`
- [x] T4: `docs/decisions/0011-kit-bundle-refactor.md`
- [x] T5: Rename `docs/design/template-bundle.md` → `docs/design/kit-bundle.md`; rewrite to describe manifest-driven layout
- [x] T6: Update `docs/specs/template-bundle.md` in place (manifest-driven invariants; filename retained)
- [ ] T7: Update index files — `docs/decisions/README.md` (+ ADR-0010, ADR-0011), `docs/specs/README.md` (+ protocols.md), `docs/plans/README.md` (+ this plan)

### Phase β — Bundle rename + flatten (commit 2)

- [ ] T8: `mkdir -p src/kanon/kit/{agents-md,sections,protocols,files}`
- [ ] T9: `git mv` tier-0 CLAUDE.md → `src/kanon/kit/files/CLAUDE.md`; remove tier-1/2/3 duplicates
- [ ] T10: `git mv` tier-1 `docs/` → `src/kanon/kit/files/docs/`; merge tier-2 `specs/` and tier-3 `design/`, `foundations/` subtrees in
- [ ] T11: `git mv` per-tier AGENTS.md → `src/kanon/kit/agents-md/tier-{0,1,2,3}.md`
- [ ] T12: `git mv src/kanon/templates/agents-md-sections` → `src/kanon/kit/sections`
- [ ] T13: `git mv src/kanon/templates/harnesses.yaml` → `src/kanon/kit/harnesses.yaml`
- [ ] T14: Remove now-empty `src/kanon/templates/` tree
- [ ] T15: Write `src/kanon/kit/manifest.yaml` (no protocols entries yet — those land in γ)

### Phase γ — Author kit.md kernel + three protocols (commit 3)

- [ ] T16: Write `src/kanon/kit/kit.md` (kernel doc with `${tier}`, `${project_name}` placeholders)
- [ ] T17: Write `.kanon/protocols/tier-up-advisor.md` (repo-root canonical, tier-min: 1)
- [ ] T18: Write `.kanon/protocols/verify-triage.md` (repo-root canonical, tier-min: 1)
- [ ] T19: Write `.kanon/protocols/spec-review.md` (repo-root canonical, tier-min: 2)
- [ ] T20: Copy byte-identical mirrors into `src/kanon/kit/protocols/`
- [ ] T21: Add `protocols` entries to `manifest.yaml`

### Phase δ — CLI refactor (commit 4)

- [ ] T22: Delete `_TIER_FILES` and `_TIER_SECTIONS` from `src/kanon/cli.py`
- [ ] T23: Add `_load_manifest()` with shape validation and caching
- [ ] T24: Rewrite `_expected_files(tier)` to derive from manifest union (+ always-present `AGENTS.md`, `.kanon/config.yaml`, `.kanon/kit.md`)
- [ ] T25: Rewrite `_build_bundle(tier, context)` to walk `kit/files/` + `kit/protocols/` filtered by manifest
- [ ] T26: Update `_assemble_agents_md(tier, project_name)` to read base from `kit/agents-md/tier-<N>.md` and sections from `kit/sections/`
- [ ] T27: Add `kit.md` scaffolding to `init` / `tier set` / `upgrade` (consumer gets `.kanon/kit.md`)

### Phase ε — AGENTS.md `protocols-index` marker (commit 5)

- [ ] T28: Add `protocols-index` to `_KNOWN_SECTIONS` in both CLI and check_kit_consistency
- [ ] T29: Write `src/kanon/kit/sections/protocols-index.md` (tier-gated variants assembled from manifest)
- [ ] T30: Insert `protocols-index` marker block into `kit/agents-md/tier-{1,2,3}.md`
- [ ] T31: Mirror tier-3 AGENTS.md content into kanon repo-root `AGENTS.md`

### Phase ζ — CI validator updates (commit 6)

- [ ] T32: `git mv ci/check_template_consistency.py ci/check_kit_consistency.py`
- [ ] T33: Drop cross-tier subset check; narrow byte-equality to the whitelist
- [ ] T34: Add manifest-path-resolution check
- [ ] T35: Add `protocols-index` to `_KNOWN_SECTIONS`
- [ ] T36: Add `kit/kit.md` existence + top-level-heading check

### Phase η — Test updates + new protocol tests (commit 7)

- [ ] T37: `git mv tests/test_template_integrity.py tests/test_kit_integrity.py`
- [ ] T38: Update path references; add `test_manifest_paths_resolve`, `test_kit_md_renders_with_placeholders`
- [ ] T39: Update `tests/test_cli.py` — parametrize protocol-file checks; add `test_kit_md_scaffolded_at_all_tiers`, `test_protocols_index_marker_present_tier1_plus`
- [ ] T40: Write `tests/test_protocols.py` — frontmatter validity, `tier-min` ↔ manifest alignment
- [ ] T41: `git mv tests/ci/test_check_template_consistency.py tests/ci/test_check_kit_consistency.py`

### Phase θ — CHANGELOG + verification (commit 8)

- [ ] T42: Append `[Unreleased]` entry to `CHANGELOG.md` (Added: protocol layer + kit.md; Changed: bundle refactor)
- [ ] T43: Run `ruff check`, `mypy --strict`, all four validators, `pytest`
- [ ] T44: Self-verify (`kanon verify /home/rosantos/workspace/platform/kanon`) ok at tier-3
- [ ] T45: Init-per-tier smoke (tier-0 through tier-3)
- [ ] T46: Migration chain (0→1→2→3→2→1→0) with user-file survival
- [ ] T47: Wheel build + `check_package_contents` ok
- [ ] T48: Dead-ref grep returns only intentional references

## Acceptance Criteria

- [ ] AC1: `src/kanon/templates/` and `tier-{0,1,2,3}/` no longer exist.
- [ ] AC2: `src/kanon/kit/` has `kit.md`, `manifest.yaml`, `harnesses.yaml`, plus `protocols/`, `files/`, `sections/`, `agents-md/` sub-buckets.
- [ ] AC3: Three protocols scaffolded at correct tiers per manifest; `.kanon/kit.md` scaffolded at every tier.
- [ ] AC4: Self-verify at tier-3 passes; init-per-tier + migration-chain pass with user-file survival.
- [ ] AC5: 45+ tests pass; coverage ≥ 70%.
- [ ] AC6: CHANGELOG `[Unreleased]` entry covers both changes.
- [ ] AC7: Fresh LLM session opening a tier-2 consumer repo discovers all three protocols via the `protocols-index` marker and `.kanon/kit.md`.

## Status notes

Phase α in flight. Commit 1 pending — completes once T1–T7 are all checked.
