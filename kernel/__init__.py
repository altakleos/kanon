"""kanon protocol substrate kernel — Python module renamed from `kanon` to `kernel` per ADR-0050 Option A.

The user-facing CLI command remains `kanon`; the distribution name remains
`kanon-core`/`kanon-kit`; the entry-point group remains `kanon.aspects`.
Only the importable Python module path changed: `from kernel.X import Y`."""

__version__ = "0.5.0a2"
